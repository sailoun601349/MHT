# 私有配置模板 —— 复制为 .env 后填写真实值（.env 已被 .gitignore 忽略，不入库）
# 生成随机密钥：openssl rand -hex 32  或  python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=please-generate-a-random-hex-value
ADMIN_PHONE=
ADMIN_CODE=
SESSION_HOURS=8
